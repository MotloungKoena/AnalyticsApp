<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

     <permission android:name="com.example.permission.VIEW_ANALYTICS"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.permission.RECEIVE_BUDGET_REMINDER" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="com.example.permission.ACCESS_FINANCE_SERVICE" />

    <uses-permission android:name="com.example.permission.READ_FINANCE_DATA" />


    <queries>
        <package android:name="motloung.koena.financeapp"/>
        <intent>
            <action android:name="motloung.koena.financeapp.ACTION_SUMMARY_SERVICE" />
        </intent>
    </queries>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnalyticsApp"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".ipc.BudgetReminderReceiver"
            android:exported="true"
            android:permission="com.example.permission.RECEIVE_BUDGET_REMINDER">
            <intent-filter>
                <action android:name="com.example.ACTION_BUDGET_REMINDER" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".AnalyticsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="motloung.koena.analyticsapp.VIEW_ANALYTICS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/Theme.AnalyticsApp.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true" />
    </application>
</manifest>
